CREATE TABLE tasks (
        id INT AUTO_INCREMENT,
        creation_date TIMESTAMP,
        due_date TIMESTAMP,
        headline VARCHAR(100),
    text VARCHAR,
    done VARCHAR(10),
    owner INT,
    task_group INT
);
